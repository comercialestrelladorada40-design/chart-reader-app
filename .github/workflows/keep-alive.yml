name: Keep Alive Ping

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping check-alert endpoint
        run: curl -s "https://chart-reader-app.onrender.com/api/check-alert?key=${{ secrets.ALERT_CHECK_SECRET }}"
